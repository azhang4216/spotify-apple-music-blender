name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build static artifact
        env:
          SPOTIFY_CLIENT_ID: ${{ secrets.SPOTIFY_CLIENT_ID }}
          SPOTIFY_REDIRECT_URI: ${{ vars.SPOTIFY_REDIRECT_URI }}
          APPLE_TOKEN_ENDPOINT: ${{ vars.APPLE_TOKEN_ENDPOINT }}
          POTATUNES_API_BASE: ${{ vars.POTATUNES_API_BASE }}
          APPLE_STOREFRONT_ID: ${{ vars.APPLE_STOREFRONT_ID }}
        run: |
          rm -rf dist
          mkdir -p dist
          cp index.html styles.css app.js preview.png dist/
          CONFIG_OUT=dist/config.js npm run build:config

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
